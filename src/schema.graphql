#import { Module } into Cosmos from "cosmos/cosmos.eth"

type Module {
    init(name: String!): InitResult!
    updateName(newName: String!): String!
    sayHello: String!
}

type InitResult {
    result: String!
}

#type SampleResult {
#    result: String!
#}
